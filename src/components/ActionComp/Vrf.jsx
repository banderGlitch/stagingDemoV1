import React from 'react';

const Vrf = () => {
    const handleVrf = () => {
        console.log('Generating Random Number...');
    };

    return (
        <div>
            <h3>Generate Random Number</h3>
            {/* <p>This action doesn't require any input fields.</p>
            <button onClick={handleVrf}>Generate</button> */}
        </div>
    );
};

export default Vrf;